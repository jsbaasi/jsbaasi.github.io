---
layout: post
title: Git Notes
date: 2026-04-28 15:57:20 +0000
categories: development
permalink: /git-notes/
---
`git rm --cached; git add; git status` Stop tracking a file. Cached option removes it from the git repo but keeps it in the working directory.

```
git revert --no-commit <hash>..HEAD
git commit
git push
```
This walks back every commit from the HEAD _until_ and not including `<hash>`.